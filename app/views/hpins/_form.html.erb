<%= form_for @hpin, html: { multipart: true } do |f| %>
  <% if @hpin.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@hpin.errors.count, "error") %> prohibited this hpin from being saved:</h2>

      <ul>
      <% @hpin.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= f.label :image %>
    <%= f.file_field :image, class: "form-control" %>
  </div>

  <div class="form-group">
    <%= f.label :description %>
    <%= f.text_field :description, class: "form-control" %>
  </div>
  <div class="form-group">
    <%= f.submit class: "btn btn-primary" %>
  </div>
<% end %>
